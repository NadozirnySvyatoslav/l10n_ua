<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Multi-company record rules (issue #178).
             Models below carry a stored company_id but had no multi-company
             ir.rule, so records leaked across ALL companies. These global
             rules scope each model to the user's allowed companies. -->

        <record id="hr_timesheet_company_rule" model="ir.access">
            <field name="name">Табель обліку: multi-company</field>
            <field name="model_id" ref="model_hr_timesheet"/>
            <field name="domain">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="operation">crud</field>
        </record>

        <record id="hr_production_calendar_company_rule" model="ir.access">
            <field name="name">Виробничий календар: multi-company</field>
            <field name="model_id" ref="model_hr_production_calendar"/>
            <field name="domain">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="operation">crud</field>
        </record>

    </data>
</odoo>
